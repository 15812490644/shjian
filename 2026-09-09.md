# 2026-09-09

## 文明6弹窗事件效果 mod(grill-with-docs session,进行中)

- 用户提供了 mod 压缩包,已解压到 `mod_src/`(注意:文件夹名为 GBK 编码,显示乱码但可正常访问)。
- 两组 mod:①弹窗事件组(3247819643 框架 / 3247827926 模板 / 3249385963 彗星示例);②特殊效果组(征服模式、上古之遗、收藏品、Mementos、通宝、六庆典、海克斯大乱斗+PVP、SpatialX退休、ZPCL、着力点v3)。
- 已派 3 个探索代理完成事实勘察,关键结论:
  - 弹窗框架支持 2~6 选项、图标、置灰、队列;选项=纯 lua 回调,可 AttachModifierByID;框架缺 .modinfo 需自建;无内置触发器/冷却,需自写 GameEvents 监听 + player Property。
  - 征服模式已预生成全部 UA/LA/UU/UB/UI 修饰器,且触发点就是占领原始首都,但**没有选择弹窗**(仅展示);改造成 N 选 1 成本低。
  - 海克斯"巨像的勇气"在 Haikesi_Modifier.sql:201;COSPLAY=遍历 TraitModifiers 全量附着。
  - 通宝/庆典/收藏品已是"N选1→attach"结构,迁移最易。
- 已创建 CONTEXT.md(术语表)+ ADR 0001(效果库自包含)、0002(以弹窗框架为底座)。

## 已敲定的设计决策(三轮 grilling)

- 交付:先文档(提取分析+效果目录+设计规格),确认后再实现 mod;效果库自包含不依赖原 mod;效果全量提取。
- 事件两分:**动态生成事件**(盟友/5城/首都三种通用触发器,4 专属选项+2 通用池递补,缺失项隐藏)、**固定选项事件**(每回合概率、世界总城市数里程碑、发现全部自然奇观等)。
- 槽位:专属最多 4 个,其余通用池递补;每次固定 1 个数值型保底选项且**随时代缩放**。
- 过滤:关键字规则("随机""额外选择""槽位""获得更多")+ 手工黑名单;深度 lua 状态类效果第一批排除。
- 去重:按"我方+目标文明+触发类型"三元组各触发一次。
- 冷却:每玩家独立 3 回合公共 CD,只约束随机/周期事件;先掷骰再查 CD;概率不暴露为配置项;每种概率触发有各自的起始时代与概率。
- 里程碑"世界总城市数"(不含城邦)达阈值 → 所有存活玩家同时弹窗各选一次,不重复。
- 占位符 6 个:{LeaderName}/{CivName}/{CivAdjective}/{CityName}/{EraName}/{PlayerLeaderName}。
- **优先考虑只有一个人类玩家的场景**。

## 触发器技术事实(已验证)

- `GameEvents.CityConquered(iNewOwner, iOldOwner, cityID, x, y)` 是最直接的占领钩子(收藏品 mod 用过);判定原始首都用 `pCity:IsOriginalCapital()`,`GetOriginalOwner() ~= playerID` 区分新建。
- 结盟**没有现成钩子**,只能回合轮询 `GetDiplomaticAI():GetDiplomaticStateIndex()` + Property 边沿检测。
- 自然奇观"全发现"无现成查询,只能 `Events.NaturalWonderRevealed` 计数 vs `GameInfo.Features.NaturalWonder` 总数。
- 时代:`Events.GameEraChanged`;回合:`GameEvents.OnGameTurnStarted`;城市总数:`PlayerManager.GetAliveMajorIDs()` + `GetCities():Members()`。

## 22:30 用户切到 handoff skill

- 已生成交接文档 `C:\Users\A\AppData\Local\Temp\civ6_popup_mod_handoff.md`(保存在 temp,不入 workspace)。
- 设计阶段未产出文档;frontier 卡在 round 4 的 4 个问题(Q1 AI玩家处理 / Q2 专属选项优先级 / Q3 实现批次 / Q4 事件schema+文档格式),下一 agent 须先问清再开工。
