# 2026-09-10

## 文明6弹窗事件效果 mod（grill-with-docs 续，round 4 完成）

接续 zip 交接（civ6_popup_mod_handoff.md），round 4 四问已答：

- Q1 AI 玩家处理 → **AI 固定选保底项**：AI 不弹窗，后台直接附着保底效果（未采纳推荐项"后台随机选"）。
- Q2 专属选项优先级 → **不截断**：推翻 round 1–3 的"最多 4 个专属选项"决策，允许 5–6 个专属选项。
- Q3 实现批次 → **三批推进**：①框架底座 modinfo + 触发器层 + 冷却层 + 三种通用触发器 + 征服模式专属选项 + 通用池接海克斯；②固定选项事件 + 通用池接通宝/庆典/收藏品/遗珍/纪念品；③lua 状态类效果。
- Q4 schema + 文档格式 → schema 字段确认（Trigger.Type / StartEra / ChancePerTurn / Once / FlavorText / Options[Type=FIXED|POOL|CIV_UNIQUE, ModifierId, Label, Fallback]）；效果目录 = Markdown 分文件 + 可筛选 HTML 总览。
- Q2 与两个既有约束冲突（框架最多 6 个选项 + 每次弹窗固定 1 个保底槽）→ round 5 追问槽位分配规则。
- Q5 槽位分配 → **阶梯让位**：专属 > 保底 > 通用池递补；专属 ≤4 补满 6、=5 仅保底、=6 占满（人类无保底槽；AI 不经槽位，后台照常拿保底效果）。
- 已落盘：ADR 0003（槽位阶梯）；CONTEXT.md 更新「动态生成事件」「保底选项」「通用池」、新增「槽位阶梯」。**frontier 已清空。**
